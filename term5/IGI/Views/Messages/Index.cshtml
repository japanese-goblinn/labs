@model Twitter.ViewModels.MessagesViewModel

@{
    ViewData["Title"] = "Messages";
}

<div class="text-left mt-3">
    <h3 class="display-6">@ViewData["Title"]</h3>
</div>
<hr/>
@foreach (var user in @Model.ChatsWith)
{
    <a>@user.UserName</a>
}